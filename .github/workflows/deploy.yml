name: 部署到Cloudflare Pages

on:
  push:
    branches:
      - main
  workflow_dispatch: # 允许手动触发

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: 检出代码
        uses: actions/checkout@v3

      # 注意：Cloudflare Pages已通过GitHub集成自动部署
      # 当代码推送到main分支时，Cloudflare会自动检测并部署
      # 无需此workflow也能自动部署
      # 此workflow仅用于显式提示部署完成

      - name: 部署完成
        run: |
          echo "✅ 代码已推送到main分支"
          echo "📚 Cloudflare Pages会自动部署"
          echo "🌐 访问地址："
          echo "   - https://ai-dashboard-6p0.pages.dev/"
          echo "   - https://bltestdata.aipush.fun/"
